--

CREATE DATABASE IF NOT EXISTS `table_list` (table_name VARCHAR(100), CONSTRAINT employees_pk PRIMARY KEY (table_name));

CREATE DATABASE IF NOT EXISTS 'students' (student_num VARCHAR(100), CONSTRAINT student_num));

CREATE DATABASE IF NOT EXISTS `student_submissions` (submissions_id int NOT NULL AUTO_INCREMENT);